# 다마고치 상태/조건 가이드

이 파일은 게임 내 각 상태가 **언제, 어떤 조건에서** 바뀌는지 정리한 문서입니다.
코드 기준: `src/lib/game/engine.ts`, `config.ts`, `types.ts`, `i18n.ts`.

## 1. 성장 (age / stage)

- `age`는 캐릭터가 **생성된 시각(`created_at`)부터 실제로 경과한 시간**으로 계산됩니다.
  `age = floor((지금 시각 - created_at) / 86400초)`
  → 하루에 한 번씩 접속하지 않아도, 며칠 만에 들어와도 정확히 반영됩니다.
- `evolutionDay`(현재 모든 캐릭터 5일, [config.ts](src/lib/game/config.ts))보다 `age`가 크거나 같아지는 순간 `stage`가 0(Kid) → 1(Adult)로 바뀌고, 화면에 진화 플래시가 뜹니다.
- age/stage가 바뀐 그 즉시 서버에 저장됩니다. (예전엔 이 시점에 저장을 안 해서, 진화한 뒤 아무 행동도 안 하고 탭을 닫으면 다음 접속 때 진화가 되돌아가는 버그가 있었음 — 수정됨)
- 디버그 모드에서 STAGE 버튼(Kid/Adult)을 누르면 `age`와 `created_at`을 그 즉시 강제로 맞춰서 스테이지를 미리 볼 수 있습니다.

## 2. 친밀도 (affinity, 0~100)

아래 행동을 할 때 오릅니다 (`engine.ts` `_select()` / `_minigamePick()`):

| 행동 | 증가량 |
|---|---|
| 먹이주기 (feed) | +1 |
| 쓰다듬기 (pet) | +2 |
| 미니게임(가위바위보) 승리 | +3 |
| 미니게임 무승부 | +1 |
| 미니게임 패배 | +0 |
| 치료 성공 (아플 때 medicine) | +1 |
| 청소 (변이 있을 때 clean) | +1 |
| 스페셜 액션 | +1 |

디버그 모드에서 AFFINITY 섹션의 버튼(0/15/40/75/100)으로 즉시 원하는 값으로 설정 가능.

### 친밀도 감소 조건

배고픔이나 행복이 **`AFFINITY_DECAY_THRESHOLD`(=2) 미만**으로 떨어져 있는 동안 친밀도가 깎입니다.
관리를 소홀히 해서 배고픔/행복이 낮아지면, 낮을수록(deficit = 2 - 값) 더 빠르게 깎이는 방식입니다:

| 배고픔 또는 행복 값 | deficit | 감소 간격 |
|---|---|---|
| 1 | 1 | `AFFINITY_DECAY_BASE_INTERVAL`(8시간)마다 -1 |
| 0 | 2 | 4시간마다 -1 (deficit에 비례해서 2배 빠름) |

- 배고픔·행복 둘 다 낮으면 **양쪽에서 각각 독립적으로** 깎여 더 빨리 감소합니다.
- 값이 2 이상으로 회복되면 그 순간부터 감소가 멈춥니다.
- 배고픔/행복 감소와 마찬가지로 실제 경과 시간 기준(오프라인 캐치업)으로 계산되므로, 앱을 안 켜놓고 방치해도 친밀도가 깎여 있을 수 있습니다.

### 친밀도 단계 (tier) — `AFFINITY_TIERS = [0, 15, 40, 75]`

| tier | 친밀도 범위 | 의미 |
|---|---|---|
| 0 | 0 ~ 14 | 아직 낯설어함 (대화 잠김 수준의 짧은 반응) |
| 1 | 15 ~ 39 | 캐주얼한 일상 대화 |
| 2 | 40 ~ 74 | 다정한 대화 |
| 3 | 75 ~ 100 | 진심 어린/애정 표현 |

상태 패널(Status)에는 친밀도가 핑크색 하트 4칸(0~100을 25 단위로 환산)으로 표시됩니다.

## 3. 대화(말풍선) 시스템 — 6가지 상황(category)

각 상황(category)마다, 위 친밀도 tier(0~3) × 정해진 줄 수 중 하나가 랜덤으로 뜹니다.
줄 수는 [config.ts](src/lib/game/config.ts)의 `DIALOGUE_LINE_COUNTS`에서 관리:

| category | tier당 줄 수 | 실행 조건 |
|---|---|---|
| `talk` | 6 | 메뉴에서 "대화" 직접 선택 |
| `idle` | 6 | **캐릭터가 먼저 말을 검** — 메뉴/미니게임/상태창/이미 뜬 말풍선이 없을 때, 90~240초(친밀도 tier당 20초씩 단축) 주기로 자동 발생. **심야(수면) 시간대엔 발생 안 함** |
| `feed` | 4 | 먹이주기 직후 35% 확률로 반응. **심야 시간대엔 반응 대사 생략**(스탯/친밀도 반영은 그대로) |
| `pet` | 3 | 쓰다듬기 직후 35% 확률로 반응. **심야 시간대엔 반응 대사 생략**(스탯/친밀도 반영은 그대로) |
| `sick` | 3 | 변이 2개 쌓여 **처음 아파지는 순간** 1회 발생 (심야에도 발생) |
| `greet` | 3 | **페이지를 열 때(세션당 1회)** 캐릭터가 먼저 인사 (심야에도 발생) |

- 말풍선은 **3초 후 자동으로 닫히거나, 가운데(S) 버튼으로 즉시 닫을 수 있습니다.**
- 확률 상수(`REACTION_CHANCE = 0.35`)와 idle 주기(`IDLE_CHAT_MIN/MAX = 90~240`)는 [config.ts](src/lib/game/config.ts)에서 조정 가능.
- `idle`/`feed`/`pet`만 심야에 억제됩니다. `talk`(직접 대화 선택)과 `sick`/`greet`은 잠든 캐릭터를 깨워서라도 보여줘야 하는 상황이라 그대로 둠.
- 대사 인덱스는 `lineIndex % 실제배열길이`로 뽑기 때문에, 특정 멤버만 새로 검증된 대사가 생겼을 때 **`DIALOGUE_LINE_COUNTS`를 늘리고 그 멤버 배열에만 추가**하면 됩니다 — 다른 멤버 배열까지 억지로 채울 필요 없이, 짧은 배열은 안전하게 앞쪽 대사를 좀 더 자주 반복할 뿐입니다.

### 대사 내용이 저장된 위치

- **공용 대사**(멤버 전용이 없을 때 쓰는 기본값): [src/lib/i18n.ts](src/lib/i18n.ts)의 `T.ko.dialogue` / `T.en.dialogue`
- **멤버 전용 대사**: 같은 파일의 `CHARACTER_DIALOGUE` 객체.
  - `kai` — 실제 Bubble 메시지 참고 (반말, ㅋㅋ 많음, "밥먹었어?" 류 캐주얼 체크인)
  - `do` — 실제 Lysn 메시지 참고. **다른 멤버와 달리 전부 존댓말**(-습니다/-세요), 음식·건강 챙김 위주
  - `sehun` — 실제 Lysn 메시지 참고. 캐주얼하지만 건강/날씨 챙기는 말 많음 ("비타민 잘 챙겨먹어야 감기 안걸려")
  - `suho` — 실제 Bubble 메시지 참고. 자기 자신을 "며니"라고 부르고, "스릉흔다"(사랑한다의 애교체) 같은 표현 사용
  - `chanyeol`, `ray`(Lay) — 실제 소스 자료가 없어서 성격만 상상으로 만든 대사 (농구/에너지 넘침 vs 춤/몽환적인 톤). 나중에 실제 스크린샷을 주시면 같은 방식으로 교체 가능
  - 여기 없는 캐릭터는 자동으로 공용 대사로 대체됨

## 4. 아픔 (sick)

- 변(poop_count)이 **2개 이상 쌓이면** 자동으로 `sick = true`가 되고, 이 순간 `sick` 카테고리 말풍선이 1회 뜹니다.
- 치료(medicine) 메뉴로 낫게 하면 `sick = false`, 친밀도 +1.
- 아픈 채로 12시간(`SICK_DEATH`) 방치하면 사망.

## 5. 사망 (alive = false)

아래 세 조건 중 하나라도 **끊기지 않고 지속**되면 사망:

| 조건 | 지속 시간 |
|---|---|
| 배고픔(hunger) 0 | 12시간 (`HUNGER_DEATH`) |
| 행복(happiness) 0 | 12시간 (`HAP_DEATH`) |
| 아픈 상태(sick) | 12시간 (`SICK_DEATH`) |

배고픔/행복은 **앱을 안 켜놓은 시간도 실제 경과 시간 기준으로 정확히 반영**됩니다 (나이 계산과 동일한 방식). 며칠 만에 다시 켜면 그동안 지났어야 할 만큼 한번에 감소하고, 0에 처음 도달한 시점도 역산해서 "그동안 이미 0이었던 시간"까지 인정됩니다 — 즉 앱을 안 켜고 있어도 위 사망 유예시간은 실제로 흘러가고, 다시 켰을 때 이미 죽어있을 수도 있습니다. (예전엔 탭이 닫혀있는 동안 감소/사망 타이머가 완전히 멈췄었음 — 수정됨)

가운데(S) 버튼으로 재시작 가능 (모든 스탯/친밀도/나이 초기화, `created_at`도 현재 시각으로 리셋).

## 6. 스탯 자연 감소

- 배고픔: 4시간마다 -1 (`HUNGER_DECAY`) — 풀 4칸 기준 16시간이면 완전히 바닥남
- 행복: 6시간마다 -1 (`HAP_DECAY`) — 풀 4칸 기준 24시간이면 완전히 바닥남
- 먹이주기/쓰다듬기(미니게임 포함)로 수치를 올리면 그 순간 다음 감소까지의 타이머도 리셋됩니다. (예전엔 값만 채우고 타이머는 안 건드려서, 감소 주기 경계 근처에서 방금 채운 직후 바로 다시 -1 되는 버그가 있었음 — 수정됨)
- 친밀도: 기본적으로 감소 없이 누적되지만, 배고픔/행복이 낮아지면 [2번 항목](#2-친밀도-affinity-0100)의 "친밀도 감소 조건"에 따라 깎임

### 심야 시간대(수면)

- **각자 기기의 로컬 시간 기준** 새벽 1시~아침 7시(`NIGHT_START_HOUR/NIGHT_END_HOUR`)에는 sleep 애니메이션으로 고정됩니다. 서버 시간이 아니라 브라우저 로컬 시간을 쓰기 때문에 나라(타임존)별로 자동으로 맞게 적용됩니다.
- 이 시간대엔 **배고픔/행복 감소가 완전히 멈춥니다** — 마치 시계가 멈춘 것처럼 취급되고, 아침 7시가 지나야 다시 흐르기 시작함.
- 오프라인으로 며칠 지났다가 다시 켜도, 그 사이 몇 번의 밤을 거쳤는지까지 정확히 계산해서 밤 시간은 전부 제외하고 감소시킵니다.
- 우선순위: **아픔(sick) > 배고픔/행복 0(sad) > 심야(sleep)** — 위급한 상태는 자는 시간에도 그대로 표시됩니다.
- 친밀도 감소·사망 유예시간(아픔/배고픔 0/행복 0이 지속된 시간)은 이 심야 정지의 영향을 받지 않고 평소처럼 흘러갑니다(범위를 배고픔/행복 자연 감소로만 한정함).

## 7. 변(poop)

- 먹이를 줄 때마다 15~30분(`POOP_MIN/MAX`) 뒤 변이 하나 생김 — **먹일 때마다 각각 독립적으로 예약**되므로, 짧은 간격으로 여러 번 먹이면 그만큼 변도 여러 개 순서대로 생김. (예전엔 예약 슬롯이 1개뿐이라 다시 먹이면 이전 예약이 사라지는 버그가 있었음 — 수정됨)
- 변이 2개 이상이면 자동으로 아픔 상태 시작.
